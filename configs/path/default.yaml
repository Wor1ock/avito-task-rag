# размещение данных и артефактов. все пути относительны корня проекта
# (hydra.job.chdir отключён в главном конфиге)
data_dir: data

# исходные датасеты (Feather)
articles: ${path.data_dir}/articles.f
calibration: ${path.data_dir}/calibration.f
test: ${path.data_dir}/test.f

# куда сохраняются построенные индексы
index_dir: ${path.data_dir}
bm25_index: ${path.index_dir}/bm25.pkl
faiss_index: ${path.index_dir}/faiss.index

# итоговый файл сабмита для тестового набора
submission: answer.csv
# предсказания калибровки в формате сабмита для анализа ошибок по запросам
calibration_answer: ${path.data_dir}/calibration_answer.csv
